# Required. Server-side only — read by server/evaluate.ts inside the Vite
# dev/preview process; never exposed to the browser (Vite only ships VITE_*).
# Evaluation is refused until this is set.
TYPESAFE_API_KEY=
