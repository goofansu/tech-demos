# Server-side only. Read by server/evaluate.ts inside the Vite dev/preview
# process; never exposed to the browser (Vite only ships VITE_* vars).
# Leave unset to run the demo in labeled mock mode.
TYPESAFE_API_KEY=
